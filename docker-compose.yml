services:
  ansh:
    build: .
    container_name: ansh
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      PORT: 8000
      CORS_ORIGINS: http://localhost:8000
    restart: unless-stopped
